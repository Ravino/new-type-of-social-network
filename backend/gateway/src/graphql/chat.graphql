type Chat {
  chatId: ID!
  ownerId: Int!
  members: RequestChatMember!
  displayName: String!
  lastMessage: Message
  messages: RequestMessage!
  createdAt: ZonedDateTime!
  updatedAt: ZonedDateTime!
}
